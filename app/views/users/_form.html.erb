<%= form_with model:user  do |f| %>
  <%= f.text_field :username, placeholder:"Enter username" %><br><div style="color:red">(username must be unique)</div>
  <br>
  <%= f.submit "Signup" , class:"btn btn-primary"%>
<% end %>