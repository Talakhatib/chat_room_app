<h1>Signup</h1>
<%= form_with model: @user do |f| %>
   <%= f.label :username%>
   <%= f.text_field :username, placeholder:"Enter a unique username.." %><br>
   <%= f.submit "Singup",class:"btn btn-primary"%>
<% end %>