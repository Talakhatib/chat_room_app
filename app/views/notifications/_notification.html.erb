
   <% counter = 0 %>
   <% notifications = Notification.where(recipient: current_user).unread %> 
   <% notifications.each do |notification| %>
      <% if notification.params[:message].room_id == room.id %>
       <% counter = counter + 1 %>
      <% end %>
   <% end %>
   <%= counter if counter > 0 %>

